<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.junmung.studyhelper">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.junmung.studyhelper.MainActivity"
            android:launchMode="singleInstance"
            android:screenOrientation="unspecified"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestActivity" />
        <activity android:name="com.example.junmung.studyhelper.memo.MemoApplyActivity" />
        <activity android:name=".drawer.SettingActivity" />
        <activity android:name="com.example.junmung.studyhelper.drawer.EmailActivity" />

        <service
            android:name=".Timer.TimerService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name="com.example.junmung.studyhelper.statistic.StudyStatisticActivity"
            >

        </activity>

        <service
            android:name="com.example.junmung.studyhelper.record.RecordService"
            android:enabled="true"
            android:exported="true"
            />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.example.junmung.studyhelper.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity android:name="com.example.junmung.studyhelper.memo.MemoOpenActivity" />
        <activity android:name="com.example.junmung.studyhelper.memo.MemoImagePagerActivity"></activity>
        <activity android:name="com.example.junmung.studyhelper.SplashActivity"></activity>
    </application>

</manifest>